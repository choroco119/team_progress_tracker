# タスク一覧 (Tasks)

- [x] `app.js`: `saveWithSync` 関数の引数拡張と挙動調整
- [x] `app.js`: 各保存イベントハンドラへの自動保存処理の追加
    - [x] 新規登録・編集 (`addProjectForm.onsubmit`)
    - [x] 削除 (`deleteProjectBtn.onclick`)
    - [x] 納入仕様書保存 (`saveSpecModalBtn.onclick`)
    - [x] 板金手配保存 (`saveSheetMetalModalBtn.onclick`)
    - [x] 部品手配保存 (`savePartsModalBtn.onclick`)
    - [x] 銘板手配保存 (`saveNameplateModalBtn.onclick`)
    - [x] 社内工事図保存 (`saveInternalDrawingsModalBtn.onclick`)
    - [x] ソフト保存 (`saveSoftwareModalBtn.onclick`)
    - [x] 設定保存 (`saveSettingsBtn.onclick`)
    - [x] 完了トグル (`completedBtn.onclick`)
- [x] 動作確認
    - [x] 共有フォルダ接続時の自動同期
    - [x] 共有フォルダ未接続時の正常動作
