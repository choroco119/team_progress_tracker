# 「部品手配」工程の実装タスク

- [x] `index.html` に「部品手配」の専用モーダルを追加する
- [x] `app.js` に「部品手配」モーダル用のDOM要素参照と変数定義を追加する
- [x] `app.js` の `renderHeader` に「部品手配」カラムを追加する
- [x] `app.js` の `renderTable` に「部品手配」セルの描画ロジックとサマリー表示 (主)・(予)・(支) を追加する
- [x] `app.js` の `renderTable` で「部品手配」のポップアップ詳細表示を追加する
- [x] `app.js` に「部品手配」モーダルの開閉・状態復元ロジック（`openPartsModal`, `renderPartsModal`）を実装する
- [x] `app.js` に「部品手配」モーダル内の各「済」ボタン・日付入力のトグル＆保存ロジックを追加する
- [x] 変更内容を検証する
