# タスク一覧 (Tasks)

- [x] `app.js`: グローバル変数 `lastFsModified` の追加
- [x] `app.js`: `loadFromFolder` で `lastFsModified` を保存するように修正
- [x] `app.js`: `saveWithSync` で保存成功時に `lastFsModified` を更新するように修正
- [x] `app.js`: `refreshIfRemoteUpdated` の比較ロジックを `!==` に修正
- [x] `app.js`: `startAutoSync` の比較ロジックを `!==` に修正
- [x] 動作確認
